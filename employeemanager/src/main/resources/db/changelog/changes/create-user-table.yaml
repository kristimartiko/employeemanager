databaseChangeLog:
  - preConditions:
        - runningAs:
            username: liquibase
  - changeSet:
      id: 01
      author: kristimartiko
      changes:
        - createTable:
            tableName: user
            -columns:
              -column:
            id: user_id
            type: bigint
            autoIncrement: true
            constraints:
              primaryKey: true
              primaryKeyName: user_pk
              nullable: false
